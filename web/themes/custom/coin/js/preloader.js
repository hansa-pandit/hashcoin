/






(function ($, Drupal) {
  'use strict';

 (window).load(function() {
        // When the page has loaded
        $("body").css('display', 'block');
      });



})(jQuery, Drupal);








/*






(function ($, Drupal, drupalSettings) {

    Drupal.behaviors.findHeight = {
      attach: function(context, settings) {
        $('window', context).on('load',function(){
          $("body").css('display', 'block');

        });
      }
    };

})(jQuery, Drupal)*/;